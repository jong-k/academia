export interface ForumType {
  id: string;
  name: string;
  place: string;
  address: string;
  host: string;
  date: string;
  time: string;
  description: string;
  image: string;
}

export interface ErrorType {
  message: string;
}
